# Computer-Use Automation System

An enterprise computer-use automation engine that discovers legacy UI workflows and compiles them into deterministic, reusable capability artifacts.

## Setup
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
playwright install chromium